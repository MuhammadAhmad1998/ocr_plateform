# Extra dependencies for the OCR/ML engine (../ocr_platform).
# Install after requirements.txt:
#   pip install -r requirements.txt -r requirements-ocr-engine.txt

celery[redis]>=5.3.6
pytesseract>=0.3.10
openai>=1.10.0
anthropic>=0.18.0
