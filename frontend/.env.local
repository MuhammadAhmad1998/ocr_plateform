# Local dev  (npm run dev + uvicorn directly): FastAPI on port 8003
# Docker dev (docker compose up):             Kong proxy on port 8100 → set to http://localhost:8100/api/v1
# LAN dev (other devices):                    http://192.168.18.6:8003/api/v1  (run API with --host 0.0.0.0)
NEXT_PUBLIC_API_URL=http://localhost:8003/api/v1
