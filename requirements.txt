fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
bcrypt>=4.1.0
python-multipart>=0.0.6
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
httpx>=0.26.0
redis>=5.0.1
pillow>=10.2.0
pypdf>=4.0.0
boto3>=1.34.0
numpy>=1.26.0,<3.0.0
accelerate>=0.27.0

# PDF handling — use pymupdf (imports as fitz). Do NOT pip install the package named "fitz".
pymupdf>=1.24.0

# ML / OCR engine (same stack as ocr_platform). For GPU torch, use PyTorch index URL.
torch>=2.4.0
torchvision>=0.19.0
transformers>=5.8.0,<5.10.0

# Optional: install full OCR engine deps from sibling folder:
#   pip install -r requirements.txt -r requirements-ocr-engine.txt
